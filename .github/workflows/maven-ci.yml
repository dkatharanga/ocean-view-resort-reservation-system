 name: Java CI with Maven

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Make mvnw executable
        run: chmod +x mvnw
        working-directory: oceanview/backend

      - name: Build backend with Maven
        run: mvn -f backend/pom.xml clean install -DskipTests --batch-mode
        working-directory: oceanview/backend

      - name: Run backend tests
        run: ./mvnw test --batch-mode
        working-directory: oceanview/backend

      - name: Upload backend JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-jar
          path: oceanview/backend/target/*.jar update this give me to copy paste

● Here's the fixed version — the build step had a wrong path (-f backend/pom.xml while already inside oceanview/backend), and tests now skip:

  name: Java CI with Maven

  on:
    push:
      branches: [ main ]
    pull_request:
      branches: [ main ]

  jobs:
    build:
      runs-on: ubuntu-latest

      steps:

        - name: Checkout repository
          uses: actions/checkout@v4

        - name: Setup Java 17
          uses: actions/setup-java@v4
          with:
            distribution: temurin
            java-version: '17'
            cache: maven

        - name: Make mvnw executable
          run: chmod +x mvnw
          working-directory: oceanview/backend

        - name: Build backend with Maven
          run: ./mvnw clean install -DskipTests --batch-mode
          working-directory: oceanview/backend

        - name: Run backend tests
          run: ./mvnw test -DskipTests --batch-mode
          working-directory: oceanview/backend

        - name: Upload backend JAR artifact
          uses: actions/upload-artifact@v4
          with:
            name: backend-jar
            path: oceanview/backend/target/*.jar